@extends('admin.layout')
@section('title', 'Kelola KK | Admin Masjid')
@section('header', 'Kelola Data KK')
@section('content')
<div class="d-flex justify-content-between align-items-end mb-4"><div><h2 class="fw-bold mb-1" style="font-family:serif">Data Kartu Keluarga</h2><p class="text-muted mb-0">No. KK menentukan kewajiban kas bulanan, bukan akun login.</p></div><div class="d-flex gap-2"><a class="btn btn-outline-success" href="{{ route('admin.families.import') }}"><i class="fa-solid fa-file-excel me-1"></i>Import Excel</a><a class="btn btn-success" href="{{ route('admin.families.create') }}"><i class="fa-solid fa-plus me-1"></i>Tambah KK</a></div></div>
<div class="card border-0 shadow-sm mb-4" style="border-radius:1rem"><div class="card-body p-3"><form method="GET" class="row g-2 align-items-end"><div class="col-md-5"><label class="form-label small">Cari Kartu Keluarga</label><input name="cari" type="search" value="{{ $cari }}" class="form-control" placeholder="No. KK, nama anggota, NIK, HP, atau alamat"></div><div class="col-md-3"><label class="form-label small">Golongan Kas</label><select name="golongan" class="form-select"><option value="">Semua golongan</option>@foreach(\App\Models\Family::TARIF_GOLONGAN as $nomor=>$tarif)<option value="{{ $nomor }}" @selected((string) $golongan === (string) $nomor)>Golongan {{ $nomor }} · Rp {{ number_format($tarif,0,',','.') }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-outline-success w-100"><i class="fa-solid fa-magnifying-glass me-1"></i>Cari</button></div>@if($cari)<div class="col-md-2"><a href="{{ route('admin.families.index', ['golongan' => $golongan]) }}" class="btn btn-outline-secondary w-100">Reset</a></div>@endif</form></div></div>
<div class="card border-0 shadow-sm" style="border-radius:1rem"><div class="card-body p-4"><div class="table-responsive"><table class="table align-middle table-hover mb-0"><thead class="table-light"><tr><th>No.</th><th>No. KK</th><th>Golongan</th><th>Kas/bulan</th><th>Anggota keluarga</th><th class="text-center">Aksi</th></tr></thead><tbody>@forelse($families as $family)<tr><td class="text-muted">{{ $loop->iteration }}</td><td class="fw-semibold">{{ $family->no_kk }}</td><td>Golongan {{ $family->golongan }}</td><td>Rp {{ number_format($family->nominal_kas, 0, ',', '.') }}</td><td><details><summary class="text-success fw-semibold" style="cursor:pointer">{{ $family->members_count }} anggota</summary><ul class="list-unstyled small mt-2 mb-0">@forelse($family->members as $member)<li class="d-flex justify-content-between gap-2 mb-1"><span>{{ $member->user?->name ?? 'Data warga tidak ditemukan' }}</span>@if($member->user)<a class="text-success text-nowrap" href="{{ route('admin.users.edit', $member->user) }}">Edit</a>@endif</li>@empty<li class="text-muted">Belum ada anggota yang terhubung.</li>@endforelse</ul></details></td><td><div class="d-flex justify-content-center gap-1"><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.families.edit', $family) }}"><i class="fa-solid fa-pen"></i></a><form method="POST" action="{{ route('admin.families.destroy', $family) }}" onsubmit="return confirm('Hapus KK ini? Anggota akan dilepaskan dari KK, tetapi akun tidak terhapus.');">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="fa-solid fa-trash"></i></button></form></div></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-4">{{ $cari ? 'Data KK tidak ditemukan.' : 'Belum ada data KK. Tambahkan KK terlebih dahulu.' }}</td></tr>@endforelse</tbody></table></div></div></div>
@endsection
